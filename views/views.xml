<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="route-optimiser.list">
      <field name="name">route-optimiser list</field>
      <field name="model">route-optimiser.route-optimiser</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="route-optimiser.action_window">
      <field name="name">route-optimiser window</field>
      <field name="res_model">route-optimiser.route-optimiser</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="route-optimiser.action_server">
      <field name="name">route-optimiser server</field>
      <field name="model_id" ref="model_route-optimiser_route-optimiser"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="route-optimiser" id="route-optimiser.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="route-optimiser.menu_1" parent="route-optimiser.menu_root"/>
    <menuitem name="Menu 2" id="route-optimiser.menu_2" parent="route-optimiser.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="route-optimiser.menu_1_list" parent="route-optimiser.menu_1"
              action="route-optimiser.action_window"/>
    <menuitem name="Server to list" id="route-optimiser" parent="route-optimiser.menu_2"
              action="route-optimiser.action_server"/>
-->
  </data>
</odoo>